import { is } from 'typescript-is';

// import Context from '../Context';
// import createAuthToken from '../utils/createAuthToken';

export async function login(): Promise<void> {
  // TODO
  // context.cookies.set('authToken', createAuthToken({ id: 1 }));
}
