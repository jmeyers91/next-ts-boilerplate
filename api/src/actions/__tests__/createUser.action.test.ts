import { createUser } from '../createUser.action';

describe('createUser.action', () => {
  test('Should run successfully with valid inputs', () => {
    // TODO: Test create user action
    // const result = await createUser.fn();
  });
});
